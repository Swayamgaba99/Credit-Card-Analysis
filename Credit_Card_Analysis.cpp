//Valid Card With Comapny Name
#include<iostream>
using namespace std

int get_size(int n){
    string_to_num=to_string(num);
    return string_to_num.length();
}
int sum_of_odd_place(int n){
    int sum=0;
    str=to_string(n);
    for(int i=get_size(str)-1;i>=0;i-=2){
        sum+=n[i]-'0';
    }
}
bool is_valid(long int n)(
    return(get_size(n)>=13 &&  get_size(n)<=16)
)
int main(){
    cout<<sum_of_odd_place(300);
}
